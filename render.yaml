services:
  - type: web
    name: mesar-youtube-downloader
    env: python
    buildCommand: pip install -r requirements.txt && apt-get update && apt-get install -y ffmpeg
    startCommand: uvicorn server:app --host 0.0.0.0 --port 
    plan: free
